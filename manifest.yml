---
declared-services:
  conversation-dev:
    label: conversation
    plan: free
applications:
- name: conversation-simple
  command: npm start
  path: .
  memory: 256M
  instances: 1
  services:
  - conversation-dev
